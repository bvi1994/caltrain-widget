export const APICALL = `http://api.511.org/transit`
export const CALTRAIN_ID = `CT`;

// The API key can be found on the https://511.org/open-data/token
// Create a file called "Tokens.js" and add the following line to
// the Tokens file without the double slashes:

// export const CALTRAIN_API_KEY = `[insert the given API key in between the quotes]`
